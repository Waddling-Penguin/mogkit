{
  "name": "mogkit-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "mogkit — the open-source toolkit for product managers. Out-craft the room.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:skills": "node scripts/validate-skills.mjs",
    "validate:graph": "node scripts/validate-graph.mjs",
    "validate:workflows": "node scripts/validate-workflows.mjs",
    "build": "pnpm --filter ./cli run build && pnpm --filter ./site run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "echo 'lint: no linter wired yet (typecheck enforces type safety)'",
    "test": "pnpm run validate:skills && pnpm run validate:graph && pnpm run validate:workflows && pnpm -r --if-present run test"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
