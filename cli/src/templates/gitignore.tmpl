.DS_Store
*.log
.env
.env.*
node_modules/
.cache/

# graph/ is generated by the `graphify` skill from sources/.
# Versioning the generated artifact is a workspace decision —
# uncomment the next two lines if you prefer to regenerate it
# from sources rather than committing it.
# graph/graph.json
# graph/graph.md
